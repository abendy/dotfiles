# Go
#
# No version manager: since Go 1.21 the default GOTOOLCHAIN=auto makes the go
# command read the `go`/`toolchain` lines in go.mod and download the matching
# toolchain on demand. Install one Go; per-project versions handle themselves.
brew "go"
