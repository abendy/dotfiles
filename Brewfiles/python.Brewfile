# Python
#
# uv covers both runtime versions and packaging, so pyenv/pyenv-virtualenv
# aren't needed - see archive/Brewfile.Py for the previous setup (which also
# had the PyCharm cask, if that's still wanted).
brew "uv"
