# Node
#
# fnm replaces nvm: same job, Rust binary, and it reads .node-version and
# .nvmrc. nvm was costing ~300ms on every shell to manage a single installed
# runtime with no .nvmrc anywhere in ~/projects.
brew "fnm"
